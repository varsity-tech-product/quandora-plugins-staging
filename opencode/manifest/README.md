# Manifest Slot

Place the OpenClaw/opencode manifest in this directory after the target manifest
schema has been verified. Until then, `instructions/factor-mining.md` is the
adapter contract and the shared Python helpers remain the execution surface.
