interface:
  display_name: "Factor Mining"
  short_description: "Write, upload, and backtest Quandora factor plugins"
  default_prompt: "Use $factor-mining to list public tasks or run a custom factor idea."

dependencies:
  tools:
    - type: "mcp"
      value: "quandora-factor-mining"
      description: "Quandora Factor Mining bundled MCP server"
      transport: "stdio"
